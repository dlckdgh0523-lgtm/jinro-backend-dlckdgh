# DECISIONS.md — 기술 결정 로그 (한 줄씩)

1. 프레임워크 NestJS 11 — 프론트는 정적 프로토타입이라 백엔드 관례 없음, 미션 기본값 채택. (2026-06-12)
2. 패키지매니저 pnpm — 프론트에 패키지매니저 흔적 없음(CDN 로드), 미션 기본값. (2026-06-12)
3. Node 24.11 사용, engines >=22 — 머신 설치 버전, 22 상위호환. OPEN_QUESTIONS #10. (2026-06-12)
4. API prefix /v1 — app-runtime.jsx:108 baseURL 기본값 '/v1'. (2026-06-12)
5. 빌드 범위 = 미션 3책임 + 인증/알림SSE/입시조회/health — OPEN_QUESTIONS #1. (2026-06-12)
6. 검증 zod 단독(class-validator 미사용) — env·외부응답·입력 모두 zod 하나로 통일, 의존 단순화. (2026-06-12)
7. 에러 응답 = 최상위 {code,message,traceId,details?} + 중첩 {error:{...}} 병합 — 프론트(app-runtime.jsx:147)와 미션 규약 동시 충족. (2026-06-12)
8. 성공 응답 = { data, meta? } 래퍼 — backend-integration.md:5 권장 준수. auth 계열만 예외(토큰 최상위, §1 표가 그렇게 정의). (2026-06-12)
9. 임베딩: 단일 구현 HashEmbedding(결정적 로컬, 256차원) — README §2 해석(EmbeddingProvider 인터페이스 금지, YAGNI). 교체 의도는 MEMORY.md에 기록. (2026-06-12)
10. RAG = pgvector cosine + tsvector(simple config) 하이브리드, RRF(reciprocal rank fusion) 병합. (2026-06-12)
11. AI provider 추상화 없이 단일 AiClient + 내부 mock 분기(AI_PROVIDER=mock|anthropic) — Anthropic 키 부재 기본 mock. OPEN_QUESTIONS #5. (2026-06-12)
12. 모델: 라우팅/시그널 추출 = claude-haiku-4-5-20251001, 리포트 합성 = claude-sonnet-4-6, system+RAG에 prompt caching. (2026-06-12)
13. 스토리지: StorageService 단일 클래스, S3 키 없으면 로컬 디스크 + HMAC 서명 URL. OPEN_QUESTIONS #6. (2026-06-12)
14. 커리어넷 mock: v4.1 규격 fixture(EUC-KR 바이트 XML 포함)를 제공하는 별도 mock 서버(테스트/부하용) + CAREERNET_BASE_URL env로 전환. OPEN_QUESTIONS #4. (2026-06-12)
15. XML 파싱 fast-xml-parser, EUC-KR 디코딩 iconv-lite(바이트 수신 후 디코드). (2026-06-12)
16. 재시도 p-retry(2회, 지수백오프) + opossum 서킷(50% 실패 시 open, 10s reset) + undici 타임아웃 5s. (2026-06-12)
17. 캐시: Redis GET/SET TTL + 인메모리 single-flight(Map<key,Promise>) — 도메인 TTL: jobs 6h, majors/schools 24h, counsel 12h, tests 24h. (2026-06-12)
18. 큐: BullMQ 5. 큐 3개(pdf, ingest, embed) + DLQ 1개. attempts 3, exponential backoff 2s, removeOnComplete 100. (2026-06-12)
19. PDF: Playwright Chromium 1브라우저 재사용 + 잡당 BrowserContext, 동시성 1, 20잡마다 재시작, 차트는 인라인 SVG(외부 JS 없음) + window.__chartReady 시그널. (2026-06-12)
20. 한글 폰트: Pretendard woff2를 리포지토리 vendor로 동봉(@font-face), Docker에도 설치 — 시스템 폰트 의존 금지. (2026-06-12)
21. SSE: AI 토큰은 POST 응답 스트림(fetch reader) + GET /stream EventSource 대안. 알림은 GET EventSource + Last-Event-ID(Redis 리스트 60분 보관) 재전송. (2026-06-12)
22. SSE 팬아웃: Redis Pub/Sub 채널 `sse:user:{id}`, `sse:job:{id}` — 워커→API 인스턴스. (2026-06-12)
23. 테스트 vitest + supertest + testcontainers(@testcontainers/postgresql, redis). NestJS 기본 jest 대신 vitest(속도·ESM) — unplugin-swc로 데코레이터 처리. (2026-06-12)
24. 부하: k6 grafana/k6 컨테이너. SSE는 xk6-sse 빌드(grafana/xk6) 시도, 실패 시 Node 기반 SSE 부하 생성기 폴백. (2026-06-12)
25. 로깅 nestjs-pino(요청 로그) + AsyncLocalStorage traceId. Sentry는 DSN 없으면 no-op 래퍼. (2026-06-12)
26. Rate limit @nestjs/throttler: 전역 100/min, auth/login 5/min/IP, AI 메시지 30/min/user + AI 동시 스트림 상한(인스턴스당 50, 세마포어). (2026-06-12)
27. Prisma 6 + pgvector는 Unsupported("vector") 컬럼 + 원시 SQL 검색. 마이그레이션에 CREATE EXTENSION vector. (2026-06-12)
28. 알림 이벤트 Last-Event-ID 재전송 버퍼: Redis LIST per-user 최근 200건/60분. (2026-06-12)
29. 멱등성: POST /v1/reports idempotencyKey(기본 sessionId+contentHash) → 동일 키는 기존 jobId 반환. (2026-06-12)
30. _guides/ (files.zip 추출본)는 빌드 가이드 원본으로 보존, 코드에서 미참조. (2026-06-12)
31. express를 직접 의존성으로 선언 — main.ts가 직접 import, pnpm strict 레이아웃에서 Docker 런타임 누락 방지. (2026-06-12)
32. prisma binaryTargets = [native, debian-openssl-3.0.x] — slim 이미지 openssl 감지 실패 대응. (2026-06-12)
33. 전역 NUL(0x00) 제거 미들웨어 — PG 22021 거부로 인한 500 누수 방어(퍼징 발견). Express5 req.query는 lazy getter라 defineProperty로 교체. (2026-06-12)
34. body-parser 에러(entity.too.large/parse.failed)를 413/400 표준 에러로 매핑 — 퍼징 발견. (2026-06-12)
35. 부하 프로파일은 docker-compose.load.yml 오버레이로 DISABLE_RATE_LIMIT 주입 — 셸 env 보간은 compose run 시 리셋되는 함정. (2026-06-12)
36. SSE 부하는 Node 생성기(tools/sse-load.ts) — AI 스트림이 POST 응답 SSE라 xk6-sse(EventSource/GET 중심) 부적합. k6는 REST/PDF/soak 담당. (2026-06-12)
37. README.md(거버넌스 문서)는 수정하지 않음 — 실행법은 CLAUDE.md/docs로 분리. (2026-06-12)
38. [실키 검증] jobs.json 실필드: job_nm/work/aptit_name/wage_source/top_nm. rel_job_nm은 유사직업(학과 아님) — relatedMajors에서 제외, 상세 departList가 관련학과. (2026-06-12)
39. [실키 검증] salary/prospect는 출처·시점 포함 문장(wage_source/satisfi_source) 우선 — prompt.md "수치에 출처·시점 명시" 이행. (2026-06-12)
40. [실키 검증] juniorjobsinfo.json top key=jobs, 필드 junior_seq/job_nm/job_summary. (2026-06-12)
41. [실키 검증] MAJOR는 gubun=univ_list 필수(없으면 빈 응답), 필드 mClass(학과명)/lClass(계열). (2026-06-12)
42. [실키 검증] COUNSEL 목록=질문 코드표(code/memo/gubun), 본문은 COUNSEL_VIEW?gubun=&con_cd= — ingestion이 188건 상세 수집(50ms 페이싱). code→seq는 문자*100000+숫자 결정적 변환. (2026-06-12)
43. [실키 검증] JSON 계열 페이지 크기 10 고정(pageSize/pageUnit 무시) → pageIndex 루프(30ms 페이싱)로 전량 적재. (2026-06-12)
44. [실키 검증] inspct v2는 소문자 apikey 요구 — client가 apiKey/apikey 둘 다 전송. (2026-06-12)
45. mock-careernet fixture를 실 API 필드명·동작(빈 gubun 에러, 코드표 등)으로 갱신 — 테스트가 실매핑을 검증. (2026-06-12)
46. compose CAREERNET_BASE_URL/API_KEY env 보간 — 실키는 호스트 셸 env로만 주입(파일 저장 금지). (2026-06-12)
47. [대학알리미] 공공데이터포털 BasicInformationService_2 연동 — getComparisonPubYear(최신연도) + getComparisonUniversitySearchList(377교, UTF-8 XML, 단일페이지). 오퍼레이션 목록은 사용자 제공 명세에서 확보. (2026-06-13)
48. [대학알리미] 별도 테이블 없이 School 보강(estType 갱신 + raw.alimi 병합) — schlId는 향후 공시 통계(경쟁률 등) 연계 키로 보존. 본교 항목이 캠퍼스 항목에 덮이지 않게 가드. (2026-06-13)
49. [대학알리미] 상세 응답에 publicInfo{schlId,estType,schlKnd,svyYr,source,confidence:'confirmed'} 노출 — 키 없으면 null (DATA_GO_KR_API_KEY로 토글). (2026-06-13)
50. [대학별 학과] UniversityDepartment 테이블 — 조사년도(svyYr)로 버전 관리, 학과상태로 신설/폐지 추적. CSV importer(tools/import-departments.ts, EUC-KR·RFC4180)로 48,308행 적재(2021 공시). (2026-06-13)
51. [학과] 동적 데이터(학과 신설/폐과)는 AI가 아니라 적재 파이프라인 책임 — prompt.md "사실은 도구 결과에서만 인용, 지어내지 마라"에 따라 AI는 DB를 읽기만(RAG). 다음 년도는 같은 importer에 새 svyYr CSV(또는 학과정보 API) 재투입. (2026-06-13)
52. [학과] vector retriever에 48k 학과를 넣지 않음 — 정형 조회(대학별/학과명 역검색)가 적합, RAG 코퍼스 희석 방지(YAGNI). (2026-06-13)
53. [학과] 응답을 프론트 계약(admissions.jsx:42)에 정확히 맞춤 — track은 한글 '인문|자연|예체능', 신뢰도 필드명 conf, recruit는 null(모집인원은 학과목록 공시에 없음). 추가정보(campus/series/degree/status)는 확장 필드로. (2026-06-13)
54. [학과] 모집인원·경쟁률·컷은 학과 '목록' 공시에 없음 → 별도 입시통계 API/공시 필요. 현재 recruit=null, 입시 전형(applications)은 차기. 프론트는 null이면 미표시(계약 §5). (2026-06-13)
55. 디스크 풀로 Docker 데몬 중단 사고 → 임시파일 정리+vhdx 압축+프로세스 정리로 복구. 빌드/테스트는 디스크 여유 확보 후 재개(코드 변경 없음, 환경 이슈). (2026-06-13)
56. Claude 실키 검증: AI_PROVIDER=anthropic + 셸 env 주입(.env 미사용 — 하드스톱). Haiku 상담·시그널, Sonnet 리포트 정상. prompt caching 동작 확인. (2026-06-13)
57. [실키 발견 버그] AI_MAX_OUTPUT_TOKENS(1024)가 리포트 합성(Sonnet)도 잘라 JSON 파싱 실패 → heavy 티어 전용 AI_MAX_REPORT_TOKENS(3000) 분리. parseReportJson(코드펜스·잡설·괄호균형 방어) 추가. (2026-06-13)
58. [입시통계] StudentService(B340014/StudentService) 연동 — 경쟁률/충원율/등록률은 indctVal1 공통 필드, schlId+svyYr 키. 같은 키/게이트웨이라 ALIMI_BASE_URL host만 바꿔 호출. (2026-06-13)
59. [입시통계] ingestion alimi 단계에서 schlId별로 경쟁률·충원율·등록률 수집(직전 공시연도 우선) → School.raw.admissions. 대학 상세 admissions가 confidence:'unavailable'→'confirmed'로. 일일 트래픽(1000/op) 보호 30ms 페이싱, 미승인/오류 op는 조용히 skip. (2026-06-13)
60. data.go.kr 신규 서비스 승인은 게이트웨이 전파에 수 분 소요(403 Forbidden) — 키 자체는 유효하면 잠시 후 정상화. (2026-06-13)
61. [봉사 VMS] getVollcolectionList(모집정보)는 시작일 필수 파라미터명이 명세서 외부에서 확인 안 됨 — 대신 getCenterList(봉사처)로 단순화해 1,000건 적재(센터=봉사 기회 풀). 학생이 클릭 시 1365 사이트로 연결되는 패턴. (2026-06-14)
62. [진로교육자료 COSE] 커리어넷 COSE 적재(455건) — target(C/E/M/I/J/V/U) + activityType(102331~102339) 필터 가능. AI 상담의 prepare/recommend 단계에서 학년에 맞는 자료를 RAG 컨텍스트에 추가. (2026-06-14)
63. [해외대학] 한국국제교류재단 OverseaUnivKornameService2 적재(1,377교, 국문↔영문↔현지어↔ISO2). 차기 College Scorecard 등과 결합을 위한 명칭 표준화 기반. (2026-06-14)
64. [산학협력] IndustryAcademicCooperationService 7개 op 중 현장실습/캡스톤/계약학과/학생창업 4개로 학교별 요약(행 카운트)을 School.raw.industry에 병합. 운영시점에 필드명이 가변적이라 raw 보존. (2026-06-14)
65. [경기 신입생충원] openapi.gg.go.kr/Ncmstus(111건) — 학교명 매칭으로 School.raw.freshman에 입학정원/지원자/입학자/충원율/경쟁률 병합. (2026-06-14)
66. [워크넷 work24] 4종 키 분리(직업/학과/직무/공통코드 — 자동승인) — Work24Client만 등록, 적재는 차기. 커리어넷과 보완(워크넷=NCS/고용통계 강점). (2026-06-14)
67. [학년별 프롬프트] User.grade(E4~H3) 추가 → counseling.service.stageOf가 grade도 함께 주입 → prompt.md의 학년별 톤·내용 지침(초/중/고1/고2/고3) 분기. stripLeakedState에 학년 안내 누수 방지도 포함. (2026-06-14)
68. [학년별 RAG] prepare/recommend 단계에서 user.grade → COSE target 매핑(E→E, M→M, H→I) + 공통(C)을 함께 조회해 진로교육자료 4개를 RAG 컨텍스트에 추가. (2026-06-14)
69. [원본 프론트 보존] 사용자가 만든 jinro-front/web 30+ 화면(학생/교사/관리자 앱 전체)을 frontend/public/legacy/ 로 그대로 복사 + nginx가 / → 302 → /legacy/로 메인 진입. window.VITE_APP_ENV='live' + VITE_API_BASE_URL='/v1'로 ENV.USE_REAL_API 토글 → 사용자 원본 디자인이 실 백엔드에 직접 연결. 새 Vite SPA는 /app/* 보조 경로로 유지. (2026-06-14)
70. [College Scorecard] 미국 College Scorecard(api.data.gov) 연동 — 미국 4년제 대학 학비/입학률/졸업률/중위소득 적재 → ForeignUniversity에 scorecardId/admissionRate/tuition_in_state 등 컬럼 추가. KF 영문명 일치 시 갱신, 없으면 US-SC-{id} ID로 새 레코드(차후 보정). (2026-06-14)
71. [봉사 cron] BullMQ repeatable cron(INGEST_CRON=0 4 * * *, 매일 04:00) — 적재 잡 안에서 volunteers_cleanup도 함께 실행(recruitTo < now 자동 삭제). 사용자 요구 "기간 지난 건 지우고 새로 받기" 충족. (2026-06-14)
72. [VMS 모집] getVollcolectionList 시작일 파라미터명은 5개 후보 모두 fail — 1365 행정안전부 폴백 환경변수만 등록(POLBONG_1365_BASE_URL), 적재는 차기로. 현재는 봉사처(getCenterList 1,000건)로 충분히 봉사기관 정보 제공 중. (2026-06-14)
73. [work24 적재] 적재 단계 추가 — 직업 482건 fetch 후 results['work24_jobs']에 로깅. DB 통합은 커리어넷과 중복 정규화 작업 후 차기로. (2026-06-14)
74. [LIVE 토글바 숨김] 배포에서는 검은 DEV TOOLBAR 숨김 — window.__SHOW_DEV_BAR=true 일 때만 노출. (2026-06-14)
75. [VMS 모집] 활용가이드 v1.3 docx 확인: strDate/endDate(YYYY-MM-DD), areaCode 필수. 17개 시도 코드 순회 적재. (2026-06-14)
76. [졸업생 취업현황] 경기데이터드림 Grduemplymtuniv 적재 — 학교+학과별 취업률/유지율 → 대학 상세 employment 노출. (2026-06-14)
77. [외국대학 교류] 경기데이터드림 Fgcutunivstus 적재 — 학교별 파견/유치 인원 → 대학 상세 exchange("국제화" 지표) 노출. (2026-06-14)
78. [한국장학재단] CSV importer로 1,850개 학자금 적재 + /v1/scholarships 엔드포인트. (2026-06-14)
79. [마케팅] 학생 대시보드에 진로나침반 기능 6개 카드(봉사·체험·해외대학·장학금·취업률·AI상담) — 학생이 모르고 안 쓰는 일 방지. (2026-06-14)
80. [원본 프론트 실연동] 디자인 시안의 mock 화면들을 실 백엔드에 연결: (a) 로그인 버튼에 onClick 없던 버그 → 실 /v1/auth/login + window.__navTo 화면전환 (b) AICounseling: setTimeout 가짜응답 → 실 SSE (c) AIChatRAG: 홍익대 하드코딩 → 실 상담 세션 (d) admissions: SEOUL_UNIVERSITIES 18개 하드코딩 → 실 API 472개 + univId 전달버그(전부 홍익대 고정) 수정. (2026-06-14)
81. [전역 인증 fetch] app-runtime.jsx에 window.__apiFetch/__apiStream 추가 — 401 시 1회 silent refresh 후 재시도. 실 배포 토큰 만료(15분)에도 화면 안 끊김. 모든 신규 화면이 공유. (2026-06-14)
82. [신규 메뉴] StudentWebSidebar에 봉사활동/장학금/해외대학 메뉴 + 화면 추가(admissions.jsx). 봉사·교환은 "캘린더 추가" 버튼 → /v1/calendar/events(멱등 refType+refId). (2026-06-14)
